1. generate the training dataset using render_dataset.py, using config/config_render.json
2. perform the homing test (including training) using mushroom_body/run_mb_homing.py, using mushroom_body/config/config_mb_homing.json
for the usd, use the ones same as the other test, or use the one called "brick_world.usd" which has huge blocks around